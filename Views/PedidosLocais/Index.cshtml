@model IEnumerable<Cantinho_Caseiro.Models.PedidosLocais>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PdcPrato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PdcQuantidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PdcCliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PdcData)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PdcAdcional)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {

        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PdcPrato)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PdcQuantidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PdcCliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PdcData)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PdcAdcional)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
